# Each route between cities will be represented by an edge
class Route:
    # constructor
    def __init__(self, destination, distance):
        self.destination = destination
        self.distance = distance
